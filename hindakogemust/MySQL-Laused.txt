CREATE TABLE 155376_hindausers(
	ID INT AUTO_INCREMENT,
	username VARCHAR(50) NOT NULL,
	password VARCHAR(90) NOT NULL,
	PRIMARY KEY (ID)
);

CREATE TABLE 155376_hindaplaces(
	ID INT AUTO_INCREMENT,
	name VARCHAR(255) NOT NULL,
	address VARCHAR(255) NOT NULL, 
	type VARCHAR(100),
	average DOUBLE DEFAULT 0,
	rated INT DEFAULT 0,
	PRIMARY KEY (ID)
);

CREATE TABLE 155376_hindarating(
	ID INT AUTO_INCREMENT,
	username VARCHAR(50) NOT NULL,
	comment VARCHAR(255),
	rating INT NOT NULL,
	place INT NOT NULL,
	PRIMARY KEY (ID)
);